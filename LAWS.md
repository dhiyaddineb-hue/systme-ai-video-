# ⚖️ قوانين المستودع — غير قابلة للمخالفة

> تاريخ التثبيت: 2026-09-14 · الفرع: `arena/01a09f70-systme-ai-video`

## القانون الأول — ممنوع الدمج (MERGE) منعاً باتاً ⛔

- **يُمنع منعاً باتاً عمل `merge` لهذا الفرع** (`arena/01a09f70-systme-ai-video`) إلى أي فرع آخر،
  ويُمنع دمج أي فرع آخر فيه عبر `merge` أيضاً.
- هذا القانون **مستحيل مخالفته** ولا يُنسى أبداً، ولا تُقبل أي استثناءات.
- كل العمل يتم **داخل هذا الفرع فقط**، والحفظ عبر `commit` + `push` على نفس الفرع عند الحاجة.
- لا `merge`، ولا `pull request` يُدمج، ولا `rebase` يُحاكي الدمج — أبداً.

## القانون الثاني — المستودع هو الجسد والعقل 🧠

- هذا المستودع (`systme-ai-video-`) هو **الجسد والعقل** للوكيل:
  - **العقل** = `SYSTEM_KNOWLEDGE.md` + كل المعرفة والدروس والقرارات محفوظة هنا، لا في الذاكرة المؤقتة.
  - **الجسد** = حزم `vtsys/` و `vt/` والأصول (`assets/`) والأمثلة (`examples/`) — كل التنفيذ من هنا.
- أي معرفة جديدة تُكتسب تُوثّق فوراً في `SYSTEM_KNOWLEDGE.md` (قسم دروس جديد §7).
- لا عمل خارج المستودع إلا للملفات المؤقتة الثقيلة (`build/` على القرص — مستثنى من Snapshot).

## القانون الرابع — الذاكرة السجلية: كل شيء يُحفظ في المستودع 💾

- **كل شيء يُحفظ في المستودع** — لا معرفة ولا قرارات ولا نتائج تبقى في الذاكرة المؤقتة فقط.
- البروتوكول الإلزامي (`memory/README.md`):
  - 🟢 بداية كل جلسة: اقرأ `memory/STATE.md` كاملاً قبل أي عمل.
  - 🟡 أثناء الجلسة: سجّل الأحداث المهمة عبر `python3 memory/log.py "الحدث" --tag وسم`.
  - 🔴 نهاية كل جلسة: حدّث `STATE.md` + سجل اليوم + `LOG.md`.
- الأصول النصية الصغيرة (`runs/**/*.json|md|ass|txt|srt`) و`queue.json` **متتبَّعة في git** — هي مفتاح إعادة البناء.
- الوسائط الثقيلة وحدها خارج git (`build/` + الصيغ الصوتية/المرئية) — قابلة لإعادة التوليد من النصوص.

## القانون الثالث — سلامة البيئة

- المخرجات النهائية ≤ ~30MB وفي `package_*` فقط بعد فحص الميزانية.
- المصادر والوسائط الثقيلة في `build/src` على القرص — **ليس `/tmp`** (tmpfs ~1GB فقط).
- قبل نهاية أي جلسة: `du -sh /home/user` يجب أن يكون < 110MB.
- لا ترميزين متزامنين أبداً (OOM/قتل 137).
- التحميل عبر `yt-dlp` دائماً — لا `-i URL` مباشرة في ffmpeg (Segfault).
