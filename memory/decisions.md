# ⚖️ سجل القرارات المعمارية

## ق-01 (2026-09-14): مصدر وحيد للحقيقة للكود
**القرار:** الكود يعيش في `vtsys/` و`vt/` فقط — `SYSTEM_KNOWLEDGE.md` يشير إليه ولا يكرره.
**السبب:** النسخ المكررة تنحرف مع الوقت (drift). الملحق أ أصبح فهرساً + سجل إصلاحات.

## ق-02 (2026-09-14): الأصول النصية الصغيرة تُتتبّع في git
**القرار:** `runs/**/*.json|md|ass|txt|srt` مستثناة من `.gitignore` + `queue.json` متتبَّع.
**السبب:** "كل شيء يُحفظ في المستودع" — هذه الملفات هي مفتاح إعادة البناء الكامل بعد ضياع الوسائط (§7.5).

## ق-03 (2026-09-14): الوسائط الثقيلة خارج git
**القرار:** `*.mp4/mp3/m4a/wav` (عدا `package_*/`) + `build/` تبقى متجاهَلة.
**السبب:** حماية سقف Snapshot (~128MB). الوسائط قابلة لإعادة التوليد من الأصول النصية.

## ق-04 (2026-09-14): الخطوط عبر GitHub API كبديل
**القرار:** `download.sh` متعدد المصادر: raw أولاً، ثم GitHub API (base64).
**السبب:** `raw.githubusercontent.com` محجوب في بعض البيئات (درس §7.7).

## ق-05 (2026-09-14): الذاكرة السجلية إلزامية
**القرار:** بروتوكول `memory/`: قراءة STATE أول الجلسة + تسجيل الأحداث + تحديث آخرها.
**السبب:** استمرارية العمل عبر الجلسات دون فقدان السياق.
